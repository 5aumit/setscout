# Domain Docs

Before exploring this repo, read the relevant material from `CONTEXT.md` and `docs/adr/`. If either does not exist, proceed silently. Use the terminology defined in `CONTEXT.md`, and explicitly flag any conflict with an ADR rather than silently overriding it.

This is a single-context repository: domain vocabulary lives in the root `CONTEXT.md`, and architecture decisions live in `docs/adr/`.
